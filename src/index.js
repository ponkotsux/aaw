import React from 'react';
import ReactDOM from 'react-dom';
import './index.css';
import GachaSimulator from './App';

ReactDOM.render(
  <React.StrictMode>
    <GachaSimulator />
  </React.StrictMode>,
  document.getElementById('root')
);
